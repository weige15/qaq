# PTB Adapter TODO

PTB is part of the paper-aligned benchmark target, but the current repo-native benchmark loader only supports built-in fake/local datasets or JSON/JSONL fixtures. Add a real PTB adapter or wire the QAQ runtime into `lm-evaluation-harness` before creating accepted PTB first-milestone configs.
